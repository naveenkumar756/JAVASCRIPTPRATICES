const user={
  name:"rohit",
  age:66,
  country:"india",
};
const keys=Object.keys(user)
console.log(keys)
const values=Object.values(user)
console.log(values)
const entires=Object.entries(user)
console.log(entires)
